int binarySearch(int* array, int lower, int upper, int target)
{
mid 
target mid
ret = binarySearch(array, lower, mid);
if(ret == -1)
ret = binarySearch(array, mid, upper);

return ret
}

int binarySearch(int* array, int lower, int upper, int target)
{
int center, range;

range = upper

}
